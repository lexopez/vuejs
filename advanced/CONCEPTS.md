# CONCEPTS OF THE LIST OF PROJECTS

## eat-n-split

- **Rendering** friends list
- **Selecting** friend from friends list
- **Adding** a friend to friends list
- **Updating** specific friend data
- **Toggling** forms
- **Two-way data binding** parent to child using **v-model**

## JobSeek-v1

- **Rendering** jobs list with **axios and json-server**
- **Rendering** specific job
- **Adding** a job to jobs list
- **Updating** specific job
- **Deleting** specific job
- **Toggling** job description **see more** and **see less** buttons
- **Routing** pages with **RouterLink** and **RouterView**
- **useRoute()** from vue-router to get **route parameter(/:id) and query parameters(?filter=value)**
- **useRouter()** from vue-router to **navigate** to different routes
- **Lifecycles Methods** for **onMounted**

## JobSeek

- **Rendering** jobs list with **axios and json-server**
- **Rendering** specific job
- **Adding** a job to jobs list
- **Updating** specific job
- **Deleting** specific job
- **Toggling** job description **see more** and **see less** buttons
- **Routing** pages with **RouterLink** and **RouterView**
- **useRoute()** from vue-router to get **route parameter(/:id) and query parameters(?filter=value)**
- **useRouter()** from vue-router to **navigate** to different routes
- **Lifecycles Methods** for **onMounted**
- **API client layer** for setting up **axios and global guards**
- **Service layer** for **fetching DATA AND CRUD OPERATIONS**
- **Interaction layer** for composables, handling logic of **API(returned data, error status, loading status) of each operation**

## pizza-menu

- **Rendering** pizza menu
- **Custom classes** for styling
- **Conditional rendering**

## Steps

- **Incrementing state**
- **Decrementing state**
- **Rendering messages array based on state value**
- **Custom classes** for styling
- **Conditional rendering**
- **Toggling** components

## Travel-list

- **Rendering** items list
- **Adding** item to item lists
- **Updating/Toggling** specific data to item
- **Deleting** specific item
- **Cutoms classes** for styling
- **Sorting** items with **computed props**
- **Computing data from items** with **computed props**

## usepopcorn

- **Searching movie**
- **Fetching movies** with **buil-in fetch method** and **watchers**
- **Rendering movies**
- **Composables** for **fetching movies**, **selecting movie with id**, **setting up movies to local storage**, **executing effect from keys events(enter & esc)**
- **Computing summary watched movies** with **computed props**
- **Adding and Rendering watched movie lists**
- **Deleting watched movie** in the watched movie lists
- **Adding Rating to watched movie**
- **Loading & Error indicator** in fetching

## vue-quiz

- **Fetching quiz questions**
- **Provide/inject** for questions state and actions
- **Conditional rendering of components(loading,error,ready,active,& finished)**
- **Composables** for useQuiz
- **Incrementing index state renders different question**
- **Conditional style for classes and toggling components based on state changes**
- **Conditional operation of state based on state changes**
- **Computed props** for specific question and answer state
- **setInterval() and clearInterval()** for timer which send event every time interval
- **Lifecycle Methods** for **onMounted and onUnmounted**

## Worldwise version 1

- **Featured-based architectures**
- **api,composables,components,features,layouts,router,store,styles,views,utils**
- **configuration-based routing**
- **children routes and rendering nested children component**
- **Protecting routes with beforeEach** for navigation guards
- **Page not found route**
- **Provide/inject** for cities data and user data
- **API LAYER, SERVICE LAYER, STORE LAYER, INTERACTION LAYER, UI LAYER**
- **Fetching and Rendering** list of cities
- **Rendering cities and rendering countries based on cities**
- **Deleting city from cities list**
- **Fetching specific city** and **Toggle the location of city on the map**
- **Adding a city from interacting on the map**
- **Marking list of cities on the map**
- **Geolocation** for locating user on the map
- **Login/logout** for users

## Worldwise version 2

- **Featured-based architectures**
- **Vuex state management** for cities data and user data
- **api,composables,components,features,layouts,router,store,styles,views,utils**
- **configuration-based routing**
- **children routes and rendering nested children component**
- **Protecting routes with beforeEach** for navigation guards
- **Page not found route**
- **API LAYER, SERVICE LAYER, STORE LAYER, INTERACTION LAYER, UI LAYER**
- **Fetching and Rendering** list of cities
- **Rendering cities and rendering countries based on cities**
- **Deleting city from cities list**
- **Fetching specific city** and **Toggle the location of city on the map**
- **Adding a city from interacting on the map**
- **Marking list of cities on the map**
- **Geolocation** for locating user on the map
- **Login/logout** for users

## Atomic-blog

- **Vuex for state management** to manage post like adding post, clearing post, searching posts
- **Rendering list of posts**
- **Searching list of posts**
- **Adding a post**

## Fast-vue-pizza

- **Pinia as state management** to manage order and cart, and user
- **Rendering list of items**
- **Adding/Removing items to cart**
- **Submit an order**
- **Search an order**

# another goal projects

- **vue with tanstack query**
